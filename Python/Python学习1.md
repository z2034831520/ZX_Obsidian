## f-string
`f-string`可以改变`print`函数输出时的输出内容，在输出字符串前面加上`f`或`F`即可直接在后续的字符串中的花括号内执行表达式或者变量嵌入，这种做法在代码编写中效率更高，同时也更加规范，因此在日常编写代码时都推荐在输出的字符串前面加上`f`
### 示例
假设我们想要输出指定内容，如设备温度，我们可以通过`f-string`的模式来实现输出指定内容
```python
print(f"设备温度为{temp_float + 1}")
```
在输出过程中我们还对