1. `socket`函数，创建一个`TCP`的套接字
2. 给`TCP`套接字绑定`ip`和端口号：服务器需要有固定的`IP`和端口号
3. 启动监听，让套接字介入可监听状态：`listen`函数（将`socket`从主动变为被动）
4. 等待客户端`SYN`连接请求(三次握手)：`accept`函数——如果客户端发起连接请求，`accept`实现和客户端三次握手，并形成新的`socket`（连接套接字），这个新的`socket`用来和客户端进行数据交互
5. `read`(`recv`)：读客户端发过来的信息/`write`（`send`）：给客户端发送信息
6. 关闭客户端的连接：
