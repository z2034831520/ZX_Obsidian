## 简介
`Shebang`这个词是由两个标点符号的英文俗称组合而成的：
- `#`在`Unix`世界里被称为**Sharp**
- `!`在`Unix`世界里被称为**Bang**
合在一起，**Sharp-Bang**读快了就变成了*Shebang*

## 运行逻辑
在`Linux`或者`macOS`系统中，文件的后缀名只是给人看的，操作系统并不关心后缀名，它判断一个文件能不能运行以及怎么运行，靠的是文件头部的数据

