## 简介
`CMake`是一个跨平台的构建系统生成工具，它本身并不会编译代码，进行代码编译的还是底层的`g++`或者`gcc`，它主要负责描述**如何构建项目**，再生成编译器需要的文件，如`Makefile`等构建文件

## 作用
在传统的`Linux`编译环境中往往都是在命令行中直接使用`gcc/g++`进行编译，在编写单文件或者简单项目时这种方法并没有什么问题，但是随着项目的复杂程度逐步增加，这种方法的短板就暴露出来了。例如：当需要同时编译多个文件时编译指令的长度就会激增，文件依赖问题无法解决