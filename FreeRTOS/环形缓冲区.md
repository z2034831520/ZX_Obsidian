## 简介
环形缓冲区的核心思想是：将一块固定大小的数组，逻辑上首尾相连成环，用写指针和读指针循环复用空间，实现`O(1)`的先进先出(`FIFO`)读写

它是`freertos`底层通信机制之一，主要用于中断和任务之间的通信，在实际的使用场景中，中断往往会被快速写入缓冲区，然后通知对应任务，任务被唤醒之后就会开始负责读取和处理数据

## 构成
环形缓冲区的构成较为简单，主要是固定数组加上两个读写指针，在运作过程中读指针和写指针独立移动，发生写操作时写指针就会指向下一个元素；发生读操作时读指针也会自动指向下一个元素。当读写指针