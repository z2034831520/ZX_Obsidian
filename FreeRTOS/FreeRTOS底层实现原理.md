## 简介
由于`FreeRTOS`是运行在单核处理器上的，单核处理器同一时刻只能运行一个任务，因此我们如果想要让单片机同时处理多个任务首要解决的应该是上下文切换的问题。`FreeRTOS`的上下文切换主要依赖以下几个机制

## 上下文切换
### 寄存器与PC指针
程序运行的本质就是`CPU`对内存、外设寄存器等存储元件的读写与计算，但是`CPU`并不能直接对内存里的数据进行计算，实际在进行计算时需要将内存中的数据移动到`CPU`周边的寄存器中，在任何一个瞬间，`CPU`只能拥有一组寄存器状态，这也就决定了它只能执行一条命令