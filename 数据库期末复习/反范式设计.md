## 简介
理论上来说数据库的范式越高。数据冗余越小，一致性越好，但是在实际的业务开发中，盲目追求高级别[范式设计](范式设计.md)会带来严重的性能问题。

高度规范化的数据库会被拆分为非常多的小表，当进行复杂查询时，需要跨表进行大量的`JOIN`连接操作，这极其消耗`CPU`的资源和磁盘`IO`，在大数据量或者高并发的场景下会导致查询速度变慢

因此在实际的开发中我们常常会使用反范式设计策略，故意在某些表中保留少量冗余数据，从而避免复杂的多表连接，以此大幅提高系统的额读取速度