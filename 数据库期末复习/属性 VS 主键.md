## 简介
在关系型数据库中，属性和主键是构成数据表的基石，简单来说，主键是从属性中挑选出来的他发挥着超出普通属性的作用

## 定义
属性：描述实体特征的指标，在数据库中，属性通常对应表中的”列“或”字段“，例如，在一张学生表中，”学号“、”姓名“、”性别“都是改表的属性

主键：能够唯一标识表中每一行记录的一个属性或一组属性的集合，它是数据记录的”唯一特征“

## 关系
### 包含关系
主键本身就是一个属性，主键是一个特殊的属性，但