## 定义
索引是建立在表的一列或者多列上的辅助存储结构，里面存放着“键值->该行物理地址”的有序对应，索引的作用就像是书本的目录一样，有了目录我们才可以快速的查找到我们想要的内容。

## 原理
为什么有了索引之后查询时的效率就能够得到大幅提升？这是因为有了索引之后查询就不需要扫描整张表了，而是先在索引里进行查找，然后再按照地址去取数据

## 缺点
索引本身要占**额外存储空间**，而且每次 `INSERT/UPDATE/DELETE` 都要**同步维护索引**，会降低写操作的效率。

## 总结
索引是一种以空间换时间的优化手段