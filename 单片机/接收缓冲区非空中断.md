## 简介
串口是每当接收到字节，并放入数据寄存器时触发

这种触发机制就会导致这种中断的触发频率会比较高，假设接收到了100个字节那么就会触发100次中断

## 使用场景
定长数据接收，以及数据量极小的场景，例如接收类似`AT\r\n` 这种需要逐个字符判断的短命令
## 清除标志位的方式
读取数据寄存器（自动清零）
## 缺点

如果波特率很高，且对方发送的是一长串连续的数据包，CPU 会被频繁打断 1000 多次，导致系统资源被严重消耗，甚至可能因为来不及处理而发生数据溢出