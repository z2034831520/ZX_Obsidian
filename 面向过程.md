## 开发方式简介
面向过程的开发思想是最经典、也是最符合人类直觉的编程范式之一，面向过程的开发方式就是把问题拆解成一系列按先后顺序执行的“步骤”（函数/过程）

面向过程开发方式的核心哲学就是：“算法 + 数据结构 = 程序”。在这个视角下，程序本身是一堆数据，加上一系列用来处理这些数据的架构步骤

## 核心特征

### 关注怎么做
面向过程的思维方式是线性的、时间轴驱动的，当你面对一个需求时，你的第一反应应该是“我第一步应该干什么？第二步应该干什么”

就像做菜一样，比如做西红柿炒鸡蛋
1. 准备食材（定义变量）
2. 洗菜（调用函数A）
3. 打鸡蛋（调用函数B）
4. 下锅炒菜（调用函数C）
5. 出锅装盘（输出结果）

### 自顶向下
面对一个复杂的系统，面向过程会采用分而治之的策略，它会将一个宏大的任务不断拆解成越来越小的子任务，直到每个子任务都能用一个简单的函数来实现
- 主函数（main）就像是一个总指挥，它本身不会实现太复杂的逻辑，主要是按照逻辑顺序依次调用底层的子函数

### 数据与逻辑分离
在面向过程的世界里，数据（变量）和操作（函数）是分开的，数据通常是公开的（如全局变量或传递给函数的参数），而函数就像是流水线上的加工机器
- 机器本身不拥有材料（数据），材料在流水线上传递，被一个又一个机器加工，最终变成成品

## 优势与局限
### 优势
1. 执行效率高：由于没有类、对象、虚函数等复杂的抽象层，面向过程的代码通常运行开销较小，运行速度快，它可以直接操作内存和底层硬件，适合对性能、内存要求较为苛刻的开发场景（如操作系统内核、底层驱动、单片机裸机开发）
2. 难度较低：对于逻辑要求简单的线性任务，面向过程的代码一眼就能看懂，没有多余的封装负担，开发速度快

### 局限
但是当程序的体积越发庞大时，其缺点就会暴露出来
1. 可维护性低：因为数据和逻辑分离，且往往依赖全局变量来共享数据，如果修改了某个全局变量的数据类型，所有用到这个全局变量的函数都要跟着改，，容易引发连锁Bug
2. 代码的复用性差：面向过程的函数往往都是为了特定任务而量身定制的，高度依赖特定上下文和数据输入，在同一项目中尚且可以进行代码复用，想要迁移到其它项目往往就不太现实了
3. 代码逻辑混乱：在没有进行全面规划就匆匆上手的项目中整体的代码逻辑会变得越来越复杂，debug时需要一步步得向下排查，中途还会跳转到其它函数中

## 总结
面向过程适用于处理具体得数学计算、底层的硬件操控和单一流程的自动化，在现代的程序开发中，面向过程和面向对象的思想往往是结合使用的；在宏观架构上使用[面向对象](面向对象.md)，在围观的每一个具体方法内部依然使用面向过程的逻辑
