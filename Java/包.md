## 简介
**包（Package）** 就像是操作系统中的**文件夹（目录）**，它不只是文件夹，本质上是类的命名空间和组织单位。
多层包之间用"."间隔
包的命名形式通常是“公司域名+项目名+模块名”，例如：com.amazon.data.entitypackage语句放在类的第一行，用来说明当前类是哪个包下的

## 作用
包主要解决三个问题
```txt
1.避免类名冲突
2.按功能组织代码
3.配合访问权限控制可见范围
```

