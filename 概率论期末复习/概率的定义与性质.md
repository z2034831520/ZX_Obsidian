## 简介
概率P(A)就是给事件A赋的一个数，衡量它发生的可能性，它满足三条公理：
1. 非负性：P(A)>0
2. 规范性：P(S)=1
3. 可列可加性：互斥事件的概率可以相加

## 性质
- 空集事件的发生概率为0
- ~P(A)=1-P(A)    <-逆事件公式，超级常用！真难则反
- 若A属于B，P(B-A) = P(B) - P(A)
- 加法公式：P(AVB) = P(A) + P(B) - P(AB)
加法公式里“减去P(AB)"是因为交集部分被计算了两遍，要扣回来


## 容斥原则
先不遗漏地把所有情况加起来，然后再把重复计算的部分减去，如果减多了就再补回来。