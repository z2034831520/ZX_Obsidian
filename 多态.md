多态是指同一个接口，使用不同的实例而执行不同的操作

## 多态的实现
在项目中我们可能会使用到多种传感器，我们可以给它们定义个一个共同的父类叫`Sensor`，在父类中我们会定义一个虚函数叫做`read_data()`

```C++
// 定义一个通用的传感器基类
class Sensor {
public:
    // 使用virtual关键字创建虚函数
    virtual void read_data() {
        std::cout << "读取传感器数据" << std::endl;
    }
    // 虚析构函数
    virtual ~Sensor() {} 
};
```

随后再让不同的传感器子类去继承这个父类，在这些子类中去重写父类中的`read_data()`虚函数

```C++
// 激光雷达继承自传感器
class Lidar : public Sensor {
public:
    // 重写基类的 read_data 方法
    void read_data() override {
        std::cout << "接收点云数据" << std::endl;
    }
};

// 摄像头继承自传感器
class Camera : public Sensor {
public:
    void read_data() override {
        std::cout << "接收图像数据" << std::endl;
    }
};
```

随后我们再声明一个父类类型的指针去指向一个子类的对象，此时再使用这些指向子类的父类指针去调用虚函数时就会返回不同的结果

```C++
int main() {
    // 1. 创建具体对象
    Lidar my_lidar;
    Camera my_camera;

    // 2. 将它们统一装进一个“基类指针”数组里
    Sensor* sensor_array[2];
    sensor_array[0] = &my_lidar;   // 父类指针指向子类 Lidar
    sensor_array[1] = &my_camera;  // 父类指针指向子类 Camera

    // 3. 统一调用，多态显现！
    for(int i = 0; i < 2; i++) {
        // 表面上我们在调用 Sensor 的 read_data
        // 但程序会在运行时查“虚函数表(V-Table)”，自动跳转到 Lidar 或 Camera 的真实代码
        sensor_array[i]->read_data(); 
    }
    return 0;
}
```

## 多态的作用

多态同样在一定程度上提高了代码的复用性，但更重要的是体现了软件工程中著名的[开闭原则](开闭原则.md)，我们在扩展新功能时不需要去修改那些已经写好的，且通过测试的老代码，这是构建大型复杂系统的基石
