## 简介
本次主要是介绍`STM32F405RGTx`系列`MCU`的定时器分布信息

## 定时器总线分布
在`STM32F405RGTx`上总共有`14`个定时器
![](assets/无人机主控MCU定时器信息/file-20260730134555865.png)
这些定时器在总线上的分布信息如下：
`APB1`：最大总线频率为`42MHz`，该总线挂载的通用定时器为`TIM2、TIM3、TIM4、TIM5`，挂在的基本定时器为`TIM6、TIM7`

`APB2`：最大总线频率为`84MHz`