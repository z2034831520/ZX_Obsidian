## 简介
在`FreeRTOS`中定时器分为硬件定时器和软件定时器，这两种定时器的应用场景和实现机制并不一样，在早期的`HAL`库裸机开发中我们只使用到了系统自带的系统滴答定时器作为系统的时钟源，当我们开始使用`FreeRTOS`时我们仍旧是使用系统的滴答定时器作为对应的时钟源。但是在裸机开发中我们称自己使用的是硬件定时器，而到了`FreeRTOS`中我们却又改口说自己使用的是软件定时器了。明明我们使用的定时器都是一样的，为什么换了一个场景叫法就不一样了呢？接下来我们就会深入的了解一下二者的差异，以及为什么需要去

## 裸机开发