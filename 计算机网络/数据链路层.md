## 定义
数据链路层的核心任务是在物理层提供的比特流基础上，提供可靠的数据传输，并确保数据能正确的从一个设备发送到另一个设备

## 功能
向网络层提供一个定义良好的服务接口
将数据封装成帧/从比特流中解析帧
错误检测和纠正
调节数据流
介质访问控制

## 帧（Frame）
在数据链路层，数据以帧的形式进行传输，每一帧通常包含以下三部分：
- 帧头：包含地址信息（源地址、目的地址）、帧类型、控制信息等
- 数据：承载来自网络层的数据报文
- 帧尾：通常包含校验和用于检测数据传输错误
![](assets/数据链路层/file-20260906101327527.png)

 网络层到数据链路层会将数据封装成帧
 数据链路层到物理层将帧转换为比特流
![](assets/数据链路层/file-20260906111920477.png)

## 字节计数法
在帧的头部添加一个字段，指明该帧的字节数
![](assets/数据链路层/file-20260906112356233.png)
### 优点
简单易实现，占用的额外开销少

### 缺点
如果计数字段的数据被传输错误，会导致数据同步错误，接收端可能会错误地解析数据

